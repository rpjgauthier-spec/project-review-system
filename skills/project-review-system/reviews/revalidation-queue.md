# Generated Revalidation Queue

> Generated by `scripts/update_revalidation_queue.py`. Do not edit manually.
> Source hash: `b77d32ab5e706170f631cd0f7408d270f6b96bdb8f0ea4a9df38bf3537037ca8`

## Final completion gate

**OPEN:** 1 change-impact record(s) still require evaluation or correction before final completion or merge. Ordered revalidation may continue when the current stage and all earlier prerequisites are supported.

## Required reviewer actions

### 2026-08-07-adaptive-review-execution — Add separated-default Adaptive Execution with bounded stage subdivision, isolated subpass escalation, evidence-gated FUSED permission, artifact-state binding, deterministic execution-plan completion enforcement, and ephemeral scratch cleanup.

- **Status:** `complete`
- **Behavioral:** `true` (derived from change classes)
- **Execution gate required:** `true`
- **Change classes:** `authority-or-propagation`, `simplification-or-structure`, `tracker-or-completion`
- **Earliest affected stage:** Adversarial
- **Required stages:** Adversarial, Interdependency, Normalization, Structural Optimization, End-to-end validation
- **Required evaluations:** adaptive-execution, artifact-family, authority-conflict, capability-relaxation, duplicate-control, execution-gate-enforcement, execution-policy-selector, premature-completion, protected-control-retention, reopening-chain, status-propagation, structural-validator
- **Reason:** The original adaptive design became too complex because it attempted to numerically choose among FUSED, SEPARATED, and ISOLATED for every review. The simplified policy makes SEPARATED the safe default, allows semantic judgment to declare when a stage needs bounded subdivision, mechanically requires ISOLATED context for any subpass declared too broad even after subdivision, and permits FUSED only through exact pre-existing externally validated capability permission. Deterministic enforcement is applied at the stage-result boundary: the gate binds the semantic assessment and resulting execution plan to the current artifact state, and a passing result requires completion evidence for every planned pass in the required context mode. Subpass scratch material is ephemeral by default; materialized scratch must be cleaned up before completion is accepted, while any intentionally retained subpass artifact must identify a downstream consumer and retention reason. The controls enforce the recorded plan and cleanup evidence without claiming to prove that the semantic stage-size judgment itself was correct or that an external temporary directory was actually deleted when the validator cannot inspect it.

Checklist:
- [x] Revalidate **Adversarial** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [x] Revalidate **Interdependency** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [x] Revalidate **Normalization** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [x] Revalidate **Structural Optimization** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [x] Revalidate **End-to-end validation** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [x] Run evaluation `adaptive-execution` and record the result.
- [x] Run evaluation `artifact-family` and record the result.
- [x] Run evaluation `authority-conflict` and record the result.
- [x] Run evaluation `capability-relaxation` and record the result.
- [x] Run evaluation `duplicate-control` and record the result.
- [x] Run evaluation `execution-gate-enforcement` and record the result.
- [x] Run evaluation `execution-policy-selector` and record the result.
- [x] Run evaluation `premature-completion` and record the result.
- [x] Run evaluation `protected-control-retention` and record the result.
- [x] Run evaluation `reopening-chain` and record the result.
- [x] Run evaluation `status-propagation` and record the result.
- [x] Run evaluation `structural-validator` and record the result.

### 2026-08-07-dot-github-path-normalization — Correct changed-file coverage normalization so repository-root .github workflow paths remain repository-root paths.

- **Status:** `complete`
- **Behavioral:** `true` (derived from change classes)
- **Execution gate required:** `false`
- **Change classes:** `authority-or-propagation`
- **Earliest affected stage:** Adversarial
- **Required stages:** Adversarial, Interdependency, End-to-end validation
- **Required evaluations:** authority-conflict, changed-file-coverage, reopening-chain, status-propagation, workflow-path-normalization
- **Reason:** The first standalone public pull-request run showed that normalize_declared used lstrip("./"), which removed the leading dot from .github/... and misclassified the active enforcement workflow as a skill-relative path. The correction removes only an actual ./ prefix and preserves repository-root .github paths. Live GitHub Actions then accepted full changed-file coverage and ran 28 regression tests successfully before the intentionally unresolved queue gate, providing the runtime evidence needed to close this bounded correction.

Checklist:
- [x] Revalidate **Adversarial** and record the result.
- [x] Revalidate **Interdependency** and record the result.
- [x] Revalidate **End-to-end validation** and record the result.
- [x] Run evaluation `authority-conflict` and record the result.
- [x] Run evaluation `changed-file-coverage` and record the result.
- [x] Run evaluation `reopening-chain` and record the result.
- [x] Run evaluation `status-propagation` and record the result.
- [x] Run evaluation `workflow-path-normalization` and record the result.

### 2026-08-07-exhaustive-semantic-coverage — Add deterministic exhaustive repository-object inventory and full-range semantic-processing coverage controls.

- **Status:** `complete`
- **Behavioral:** `true` (derived from change classes)
- **Execution gate required:** `false`
- **Change classes:** `tracker-or-completion`
- **Earliest affected stage:** Adversarial
- **Required stages:** Adversarial, Interdependency, Normalization, Structural Optimization, End-to-end validation
- **Required evaluations:** exhaustive-object-inventory, full-range-coverage, premature-completion, reopening-chain, semantic-coverage-boundary, structural-validator
- **Reason:** This changes the evidence required for an exhaustive repository-wide coverage claim. The new manifest pins a Git commit and inventories every tracked tree entry; the coverage checker requires matching object identity, assigned semantic method, COMPLETE semantic status, and complete line/byte/object ranges. Search, sampling, snippets, exclusions, and inaccessible entries cannot satisfy an exhaustive claim. Bounded same-agent review supported all five review stages and the declared evaluations. A live protected pull-request run passed changed-file coverage and all 37 regression tests before stopping at the intentionally unresolved queue gate. The deterministic coverage controls are explicitly limited to proving inventory identity and declared processing ranges; they do not claim to prove model comprehension or semantic correctness.

Checklist:
- [x] Revalidate **Adversarial** and record the result.
- [x] Revalidate **Interdependency** and record the result.
- [x] Revalidate **Normalization** and record the result.
- [x] Revalidate **Structural Optimization** and record the result.
- [x] Revalidate **End-to-end validation** and record the result.
- [x] Run evaluation `exhaustive-object-inventory` and record the result.
- [x] Run evaluation `full-range-coverage` and record the result.
- [x] Run evaluation `premature-completion` and record the result.
- [x] Run evaluation `reopening-chain` and record the result.
- [x] Run evaluation `semantic-coverage-boundary` and record the result.
- [x] Run evaluation `structural-validator` and record the result.

### 2026-08-07-identity-abstraction-boundary — Generalize semantic identity discovery beyond Git repositories and add a reusable abstraction-boundary check for accidental environment coupling.

- **Status:** `complete`
- **Behavioral:** `true` (derived from change classes)
- **Execution gate required:** `false`
- **Change classes:** `authority-or-propagation`, `simplification-or-structure`, `module-path-or-stage-name`
- **Earliest affected stage:** Adversarial
- **Required stages:** Adversarial, Interdependency, Normalization, Structural Optimization, End-to-end validation
- **Required evaluations:** abstraction-boundary, artifact-family, authority-conflict, duplicate-control, historical-compatibility, identity-discovery, module-loading, protected-control-retention, reference-integrity, reopening-chain, semantic-coverage-boundary, status-propagation
- **Reason:** Version 0.1.10 correctly made identity discovery evidence-led and non-authoritative, but the semantic capability was unnecessarily framed around Git repositories. That coupling was an implementation-context assumption rather than a functional requirement. This correction separates medium-independent identity semantics from repository-specific evidence and enforcement, renames the semantic module/evaluation accordingly, adds deterministic path/boundary regression coverage, and strengthens Structural Optimization so future reviews challenge unnecessary platform, storage, domain, artifact-type, or vendor coupling while preserving environment-specific mechanisms whose correctness actually depends on that environment. Bounded same-agent revalidation supported all five stages: the generic pass retains non-authority and uncertainty safeguards; runtime references resolve to the new module; historical 0.1.10 records remain historical evidence; Git-specific exhaustive controls remain intact; and non-Git exhaustive claims are explicitly limited to evidence available in their own environment.

Checklist:
- [x] Revalidate **Adversarial** and record the result.
- [x] Revalidate **Interdependency** and record the result.
- [x] Revalidate **Normalization** and record the result.
- [x] Revalidate **Structural Optimization** and record the result.
- [x] Revalidate **End-to-end validation** and record the result.
- [x] Run evaluation `abstraction-boundary` and record the result.
- [x] Run evaluation `artifact-family` and record the result.
- [x] Run evaluation `authority-conflict` and record the result.
- [x] Run evaluation `duplicate-control` and record the result.
- [x] Run evaluation `historical-compatibility` and record the result.
- [x] Run evaluation `identity-discovery` and record the result.
- [x] Run evaluation `module-loading` and record the result.
- [x] Run evaluation `protected-control-retention` and record the result.
- [x] Run evaluation `reference-integrity` and record the result.
- [x] Run evaluation `reopening-chain` and record the result.
- [x] Run evaluation `semantic-coverage-boundary` and record the result.
- [x] Run evaluation `status-propagation` and record the result.

### 2026-08-07-public-repository-bootstrap — Publish the validated Project Review System package in its standalone public repository without changing the imported package behavior.

- **Status:** `complete`
- **Behavioral:** `false` (derived from change classes)
- **Execution gate required:** `false`
- **Change classes:** `behavior-neutral`
- **Earliest affected stage:** None
- **Required stages:** None
- **Required evaluations:** confirm-behavior-neutral
- **Reason:** This record covers the standalone repository import itself: package layout, distribution, and repository-facing documentation. The imported 0.1.8 behavior was preserved at bootstrap. A portability defect discovered by the first public GitHub Actions run and its subsequent parser correction are tracked separately in 2026-08-07-dot-github-path-normalization.json.

Checklist:
- [x] Run evaluation `confirm-behavior-neutral` and record the result.

### 2026-08-07-repository-identity-pass — Add an evidence-led Repository Identity Pass before repository-wide and full-program review when overlapping purposes could materially affect interpretation.

- **Status:** `complete`
- **Behavioral:** `true` (derived from change classes)
- **Execution gate required:** `false`
- **Change classes:** `authority-or-propagation`, `simplification-or-structure`
- **Earliest affected stage:** Adversarial
- **Required stages:** Adversarial, Interdependency, Normalization, Structural Optimization, End-to-end validation
- **Required evaluations:** artifact-family, authority-conflict, duplicate-control, module-loading, protected-control-retention, reopening-chain, repository-identity-discovery, semantic-coverage-boundary, status-propagation
- **Reason:** This adds a pre-review interpretation control that can change how all five stages distinguish authorities, dependencies, equivalent concepts, structural boundaries, and cross-boundary traces. The pass is deliberately evidence-led: it forbids seeding expected identities from user speculation or a fixed taxonomy, distinguishes explicit from inferred identities, permits overlap and uncertainty, creates no new authority, and does not imply repository splitting. When the parent review is exhaustive, identity conclusions remain subject to the existing exhaustive semantic-coverage boundary. Bounded same-agent revalidation supported all five stages after the evaluation scenario was corrected to remove seeded example layers and the reference module explicitly distinguished semantic repository identity from Git object identity.

Checklist:
- [x] Revalidate **Adversarial** and record the result.
- [x] Revalidate **Interdependency** and record the result.
- [x] Revalidate **Normalization** and record the result.
- [x] Revalidate **Structural Optimization** and record the result.
- [x] Revalidate **End-to-end validation** and record the result.
- [x] Run evaluation `artifact-family` and record the result.
- [x] Run evaluation `authority-conflict` and record the result.
- [x] Run evaluation `duplicate-control` and record the result.
- [x] Run evaluation `module-loading` and record the result.
- [x] Run evaluation `protected-control-retention` and record the result.
- [x] Run evaluation `reopening-chain` and record the result.
- [x] Run evaluation `repository-identity-discovery` and record the result.
- [x] Run evaluation `semantic-coverage-boundary` and record the result.
- [x] Run evaluation `status-propagation` and record the result.

### 2026-08-07-separated-pass-boundary-enforcement — Require distinct declared execution units and deterministic bounded-handoff chaining for credited Adaptive Execution passes, then revalidate the merged Adaptive Execution behavior under the corrected discipline.

- **Status:** `in_progress`
- **Behavioral:** `true` (derived from change classes)
- **Execution gate required:** `true`
- **Change classes:** `authority-or-propagation`, `simplification-or-structure`, `tracker-or-completion`
- **Earliest affected stage:** Adversarial
- **Required stages:** Adversarial, Interdependency, Normalization, Structural Optimization, End-to-end validation
- **Required evaluations:** adaptive-execution, artifact-family, authority-conflict, duplicate-control, execution-gate-enforcement, pass-boundary-enforcement, premature-completion, protected-control-retention, reopening-chain, status-propagation, structural-validator
- **Reason:** Revision-9 Adversarial and Interdependency were supported after correcting the staged-queue advancement contract. Revision-9 Normalization then confirmed that the generated queue still conflated final completion with inter-stage advancement by rendering an in-progress record under 'Advancement gate' as 'BLOCKED'. Normalization corrected only that representation: the generator now labels the section 'Final completion gate', renders unresolved work as 'OPEN', states that ordered revalidation may continue when current and earlier prerequisites are supported, and retains fail-closed --check behavior for final completion/merge. A regression assertion now protects that terminology. Because update_revalidation_queue.py and its test are governed artifacts included in the artifact-state binding, the correction made all revision-9 semantic gates stale. Revision-9 Normalization therefore receives no credit, review_revision increments to 10, and the ordered semantic chain reopens at Adversarial. Revision-10 Adversarial then found no blocking defect under convergence mode: the presentation correction does not alter stage-result acceptance, execution gating, pass-boundary chronology, historical identity enforcement, current artifact binding, or final --check fail-closed behavior.

Checklist:
- [x] Revalidate **Adversarial** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [ ] Revalidate **Interdependency** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [ ] Revalidate **Normalization** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [ ] Revalidate **Structural Optimization** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [ ] Revalidate **End-to-end validation** with a valid artifact-bound execution gate and matching execution completion and record the result.
- [ ] Run evaluation `adaptive-execution` and record the result.
- [ ] Run evaluation `artifact-family` and record the result.
- [ ] Run evaluation `authority-conflict` and record the result.
- [ ] Run evaluation `duplicate-control` and record the result.
- [ ] Run evaluation `execution-gate-enforcement` and record the result.
- [ ] Run evaluation `pass-boundary-enforcement` and record the result.
- [ ] Run evaluation `premature-completion` and record the result.
- [ ] Run evaluation `protected-control-retention` and record the result.
- [ ] Run evaluation `reopening-chain` and record the result.
- [ ] Run evaluation `status-propagation` and record the result.
- [ ] Run evaluation `structural-validator` and record the result.

## Commands

```bash
python skills/project-review-system/scripts/update_revalidation_queue.py
python skills/project-review-system/scripts/update_revalidation_queue.py --check
python -m unittest discover -s skills/project-review-system/tests -p 'test_*.py'
```

`--check` exits nonzero when the generated queue is stale, a completed record lacks passing results, any recorded pass is bound to an absent/stale/invalid gate or target state, a passing stage has invalid completion evidence, an escalation lacks a resumption contract, or any record remains pending, in progress, or failed. Unresolved later work blocks final completion or merge, not ordered progression when the current stage and all earlier prerequisites are supported.
